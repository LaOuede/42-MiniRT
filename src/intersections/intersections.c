#include "minirt.h"

/* float	search_intersection(t_object *obj)
{
	float	t;

	if (obj->type == SPHERE)
		t = hit_sphere(obj);
	else if (obj->type == PLAN)
		t = hit_plane(obj);
	else if (obj->type == CYLINDRE)
		t = hit_cylinder(obj);
} */
